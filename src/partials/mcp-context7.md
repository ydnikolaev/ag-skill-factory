## MCP Context7

> **Use `mcp_context7`** for library docs:
> - Check `mcp.yaml → context7.default_libraries` for pre-configured libs
> - Call `resolve-library-id` first, then `query-docs`

<!-- TODO: add usage examples for different skills -->
