# Document Enums
# SSOT for document-specific enum values
# Shared enums: ../shared/enums.yaml (statuses, lifecycles, categories, phases)

# This file exists for document-specific extensions only.
# All common enums are in shared/enums.yaml
