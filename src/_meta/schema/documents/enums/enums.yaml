# Document Enums
# SSOT for document-specific enum values
# Shared enums: ../../shared/factory.yaml (phases, categories)
# Shared enums: ../../shared/runtime.yaml (statuses, lifecycles, triggers, mcp_servers)

# This file exists for document-specific extensions only.
# All common enums are in shared/*.yaml
