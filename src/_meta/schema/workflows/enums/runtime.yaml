# Workflow Runtime Enums
# SSOT for workflow-related enum values (used at execution time)

# Turbo modes (auto-execution annotations)
turbo_modes:
  - turbo       # Auto-run single step
  - turbo-all   # Auto-run all steps in workflow
