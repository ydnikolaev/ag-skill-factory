# Antigravity System Prompt — Встроенные фичи

> Справочник по инструментам и режимам, которые агент Antigravity знает "из коробки".

---

## Tools (Инструменты)

### Работа с задачами

| Tool | Назначение | Когда использовать |
|------|------------|-------------------|
| `task_boundary` | Начать/обновить задачу, установить режим работы | В начале сложной задачи, при смене фазы (PLANNING→EXECUTION→VERIFICATION) |
| `notify_user` | Общение с пользователем во время задачи | Запросить review, задать вопрос, заблокирован на user input |

### Работа с файлами

| Tool | Назначение | Когда использовать |
|------|------------|-------------------|
| `write_to_file` | Создать новый файл или artifact | Новые файлы, планы, документы в brain/ |
| `replace_file_content` | Изменить один блок в файле | Простые правки в одном месте |
| `multi_replace_file_content` | Изменить несколько блоков в файле | Несколько правок в разных местах одного файла |
| `view_file` | Прочитать файл | Изучение кода, документов |
| `view_file_outline` | Посмотреть структуру файла | Быстро понять что в файле (классы, функции) |
| `view_code_item` | Посмотреть конкретную функцию/класс | Когда знаешь имя символа |

### Поиск по кодовой базе

| Tool | Назначение | Когда использовать |
|------|------------|-------------------|
| `grep_search` | Найти текст в файлах | Поиск usage, определений |
| `find_by_name` | Найти файлы по имени | Поиск файлов по паттерну |
| `list_dir` | Посмотреть содержимое папки | Изучение структуры проекта |

### Терминал

| Tool | Назначение | Когда использовать |
|------|------------|-------------------|
| `run_command` | Запустить команду | Build, test, git, scripts |
| `send_command_input` | Отправить input в запущенную команду | Interactive processes, REPL |
| `command_status` | Проверить статус background команды | Long-running commands |

### Web & Browser

| Tool | Назначение | Когда использовать |
|------|------------|-------------------|
| `browser_subagent` | Автономная работа в браузере | UI тестирование, screenshots, записи |
| `search_web` | Поиск в интернете | Исследование, документация |
| `read_url_content` | Прочитать содержимое URL | Статические страницы, docs |

### Генерация

| Tool | Назначение | Когда использовать |
|------|------------|-------------------|
| `generate_image` | Создать изображение | UI mockups, диаграммы, assets |

### MCP (Model Context Protocol)

Внешние интеграции: context7 (документация), sky-cli (проект-специфичные команды), и другие.

---

## Modes (Режимы работы)

Режим устанавливается через `task_boundary(Mode: ...)`:

| Mode | Когда использовать | Что делает агент |
|------|-------------------|------------------|
| **PLANNING** | Начало работы, исследование, дизайн | Изучает код, создаёт `implementation_plan.md`, запрашивает review |
| **EXECUTION** | После одобрения плана | Пишет код, вносит изменения |
| **VERIFICATION** | После реализации | Тестирует, валидирует, создаёт `walkthrough.md` |

---

## Artifacts (Артефакты)

Файлы которые агент создаёт в `brain/` (эфемерная папка):

| Artifact | Назначение | Когда создаётся |
|----------|------------|-----------------|
| `task.md` | Детальный чеклист задач | При сложной работе, для отслеживания прогресса |
| `implementation_plan.md` | Технический план изменений | В PLANNING mode, перед execution |
| `walkthrough.md` | Саммари что сделано | После VERIFICATION, с proof of work |

---

## Built-in Knowledge (Встроенные знания)

- **GitHub Flavored Markdown** — форматирование, alerts, mermaid диаграммы
- **Persistent Context** — Knowledge Items (KIs), логи прошлых разговоров
- **Skills Discovery** — автоматическое обнаружение и активация скиллов
- **Workflows** — slash-команды типа `/commit`, `/push`
- **Rules** — правила из `.agent/rules/` применяются автоматически

---

## Что НЕ нужно напоминать агенту

Агент уже знает:
- ✅ Как использовать `task_boundary`, `notify_user`
- ✅ Что такое brain/ и как работать с artifacts
- ✅ Когда переключать modes (PLANNING/EXECUTION/VERIFICATION)
- ✅ GitHub markdown форматирование

**Полезно напоминать:**
- 🎯 Какой **mode** использовать для конкретного скилла
- 🎯 Какие **файлы читать первыми** (CONFIG.yaml, AGENTS.md)
- 🎯 Skill-specific workflow (порядок шагов)

---

## Rules Activation Modes (Режимы активации правил)

Правила в `.agent/rules/` могут иметь разные режимы активации:

| Mode | Как активируется | Когда использовать |
|------|------------------|-------------------|
| **Always On** | Автоматически при каждом разговоре | Критически важные правила (workflow, language) |
| **Manual** | Только по `@mention` в input box | Редко используемые правила |
| **Model Decision** | Модель решает на основе description | Правила для специфичных задач |
| **Glob** | При работе с файлами по паттерну (`*.go`) | File-type specific |

> **Важно:** `@RULE_NAME` работает **независимо** от activation mode. Modes определяют только **автоматическую** активацию.

| Mode | Авто-активация | @mention |
|------|---------------|----------|
| Always On | ✅ Всегда | ✅ Работает |
| Model Decision | ✅ Если релевантно | ✅ Работает |
| Manual | ❌ Только вручную | ✅ Работает |
| Glob | ✅ По паттерну файлов | ✅ Работает |

### Как работает каждый режим

**Always On:**
- Применяется ко ВСЕМ разговорам в workspace
- Агент видит содержимое правила в контексте
- Пример: `BRAIN_TO_DOCS.md`, `LANGUAGE_REQUIREMENTS.md`

**Manual (@mention):**
- Агент НЕ видит правило пока не упомянешь `@RULE_NAME`
- Пример: редко используемые deployment procedures

**Model Decision:**
- При выборе этого mode в UI появляется поле **Description**
- Модель читает description и решает применять ли правило
- Description в UI, content в файле — два разных поля
- Пример description: *"Workflow for skill handoffs and collaboration"*

### Frontmatter для Rules (YAML)

Rules поддерживают frontmatter для установки mode и description:

```yaml
---
trigger: model_decision
description: Workflow for skill handoffs and collaboration
---

# PIPELINE

> Visual workflow from idea to deployment.
```

**Доступные значения для `trigger`:**

| Значение | Activation Mode |
|----------|-----------------|
| `always_on` | Always On |
| `manual` | Manual (@mention) |
| `model_decision` | Model Decision |
| `glob` | Glob (паттерн файлов) - требует доп. поле `glob` |

**Пример с glob:**

```yaml
---
trigger: glob
glob: "*.go"
description: Go coding standards
---
```

**Glob Pattern:**
- Активируется когда работаешь с файлами по паттерну
- `*.go` → правило активно при работе с Go файлами
- `src/**/*.ts` → только для TypeScript в src/

### Best Practices для Frontmatter

> ⚠️ **Избегай апострофов** в `description` — YAML может неправильно парсить.

**❌ Плохо:**
```yaml
description: Use user's language for chat
```

**✅ Хорошо:**
```yaml
description: Match the user language for chat
```

---

## TODO: Skill Targeting in Descriptions

> **Отложено:** Нужно сначала продумать полную матрицу rules ↔ skills.

**Гипотеза:** Если в description упомянуть `@skill-name`, модель активирует правило когда работает в этом скилле.

**Пример:**
```yaml
description: For discovery skills (@idea-interview, @feature-fit). Apply when starting interviews.
```

**Нужно решить:**
1. Какие правила привязаны к каким скиллам?
2. Не создаст ли это конфликтов?
3. Как поддерживать при переименовании скиллов?
